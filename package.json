{
  "name": "playwright-accelerator",
  "description": "Accelerator to speed up Playwright/Jest testing",
  "license": "MIT",
  "repository": "https://github.com/nearform/playwright-accelerator",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint . --ext .js,.jsx",
    "prepare": "husky install",
    "test": "npx playwright test --workers 4 --reporter=list"
  },
  "devDependencies": {
    "@commitlint/cli": "^15.0.0",
    "@commitlint/config-conventional": "^15.0.0",
    "@playwright/test": "^1.17.1",
    "eslint": "^8.4.1",
    "eslint-plugin-playwright": "^0.6.0",
    "husky": "^7.0.4",
    "lint-staged": "^12.1.3",
    "playwright": "^1.17.1"
  },
  "lint-staged": {
    "*.{js,jsx}": "eslint --cache --fix"
  }
}
